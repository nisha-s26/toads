/** Shared page width + horizontal padding (mobile/tablet gutters via `.page-x-gutter` in index.css). */
export const pageSectionContainer =
  "mx-auto min-w-0 max-w-9xl page-x-gutter lg:px-8 xl:px-10 2xl:px-14"
